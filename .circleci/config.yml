version: 2
jobs:
  build:
    docker:
      - image: node:8.7.0
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: yarn
      - run:
          name: Build packages
          command: yarn build
      - run:
          name: Checks
          command: yarn check-all
  deploy:
    docker:
      - image: node:8.7.0
    steps:
      - checkout
      - run:
          name: Set up auth token
          command: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
      - run:
          name: Install dependencies
          command: yarn
      - run:
          name: Build packages
          command: yarn build
      - run:
          name: Publishes packages
          command: ./node_modules/.bin/lerna publish --skip-git --canary --yes

workflows:
  version: 2
  build-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          # filters:
          #   branches:
          #     only: master
