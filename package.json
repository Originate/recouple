{
  "name": "safe-api",
  "version": "0",
  "description": "typesafe api routing",
  "main": "src/index.js",
  "license": "MIT",
  "devDependencies": {
    "eslint-plugin-flowtype": "^2.39.1",
    "flow-typed": "^2.2.3",
    "jest": "^21.2.1"
  },
  "dependencies": {
    "babel-eslint": "^8.0.2",
    "eslint": "^4.11.0",
    "eslint-plugin-import": "^2.8.0",
    "eslint-plugin-jest": "^21.3.2",
    "flow-bin": "^0.55.0",
    "prettier": "^1.8.2"
  },
  "scripts": {
    "check-all": "yarn format-check && yarn lint && flow check && jest",
    "lint": "eslint *.js src/**/*.js",
    "format": "prettier --write *.js src/**/*.js",
    "format-check": "prettier -l *.js src/**/*.js"
  }
}
